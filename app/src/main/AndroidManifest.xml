<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cit17b.studays">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <service
            android:name=".note.DeleteNoteService"
            android:exported="false"></service>
        <receiver android:name=".note.CreateNoteActivity$AlarmReceiver"/>

        <activity
            android:name=".note.CreateNoteActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".note.NoteListActivity"
            android:label="@string/notes"
            android:screenOrientation="portrait" />
        <activity
            android:name=".lesson.DayOfTheWeekDialog"
            android:label="@string/day_of_the_week"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert" />
        <activity
            android:name=".lesson.LessonTypeDialog"
            android:label="@string/lesson_type"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert" />
        <activity
            android:name=".lesson.CreateLessonActivity"
            android:label="@string/create"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>